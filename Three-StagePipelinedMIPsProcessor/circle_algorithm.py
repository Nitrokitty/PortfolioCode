import math

def circ(x, y, direction, radius, c_x, c_y):
	if(x == radius):
		direction = -direction
	new_x = x + direction
	new_y = math.sqrt(radius * radius - new_x * new_x)
	new_x + new_x + center
	new_y = new_y + center
	
